{
  "name": "javascript-testing",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --coverage",
    "build": "mkdir -p dist && cp -r * dist/"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0"
  },
  "dependencies": {
    "sweetalert2": "^11.14.5"
  }
}

